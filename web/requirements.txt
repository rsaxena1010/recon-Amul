# No third-party dependencies.
#
# The hosted app (app.py) is pure Python standard library on purpose: the Blinkit
# Apps build cluster's pip package proxy cannot deliver wheels, so any `pip install`
# fails. The Dockerfile therefore does NOT run pip.
#
# The notebook path (dashboard_core.py) DOES use pandas/numpy/pyarrow — install
# those in your notebook/Jupyter environment, not in this container:
#   pandas  numpy  pyarrow
